# Security configurations
audit=true
fund=false
package-lock=true

# Strict SSL
strict-ssl=true

# Prefer offline installs for security
prefer-offline=false

# Engine strict - enforce node/npm versions
engine-strict=true

# Avoid optional dependencies when possible
omit=optional

# Logging
loglevel=warn
